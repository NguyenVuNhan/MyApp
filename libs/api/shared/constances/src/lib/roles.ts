export enum Role {
  // Admin user
  Admin = 'Admin',
  // Standard user, use for users who already have an account
  User = 'User',
  // Guest user, who hasn't create any account yet.
  Guest = 'Guest',
}
